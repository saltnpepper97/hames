# Project Stats

Smoke plugin for M09. Counts files in the trusted project via
`broker:project_read` / `project.list`. It never touches `$HOME` or Hames
state directly.

Tool id: `project-stats.summary`.
